import 'package:flutter/widgets.dart';

/// Reserves a space in a chat message body to show the date overlay above it.
class DatePushWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(height: 15, width: 1);
  }
}
